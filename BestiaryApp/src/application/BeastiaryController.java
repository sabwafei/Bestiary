package application;

import javafx.event.ActionEvent;
import javafx.fxml.FXML;
import javafx.scene.control.Button;

public class BeastiaryController {
	@FXML
    private Button createMonBtn;
	
	//action event for UI Buttons
	@FXML
	void createMonster(ActionEvent event) {
		//create monster method will first enable the text fields where the user can create a new monster to add to the beastiary
		
	}
}
